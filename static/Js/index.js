const headerTitle = document.querySelector('.logo h1');
headerTitle.textContent = "ALX Study Buddy Hub";

// Example: Adding animation to the hero section
const heroSection = document.querySelector('.hero');

// Add a CSS class to trigger the animation
heroSection.classList.add('animate');

// Example: Adding animation to the features section
const featuresSection = document.querySelector('.features');

// Add a CSS class to trigger the animation
featuresSection.classList.add('animate');